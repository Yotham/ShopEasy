// pages/generate.js
import GenerateScreen from '../app/components/GenerateScreen'; // Adjust the path as necessary to point to your FAQ component

const GeneratePage = () => {
    return <GenerateScreen />;
};

export default GeneratePage;
