// pages/account-settings.js
import AccountSettings from '../app/AccountSettings'; // Adjust the path as necessary to point to your FAQ component

const FaqPage = () => {
    return <AccountSettings />;
};

export default FaqPage;
