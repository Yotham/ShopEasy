To Run:
1. Run "npm install" in project root
2. Run "npx expo start" in project root
3. Install expo go app on your mobile device and log in
4. Scan QR code from terminal in camera app (IOS) or expo app (Andriod)
